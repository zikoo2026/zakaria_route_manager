
**مشروع Flutter لإدارة التنقل بين الشاشات باستخدام Route Manager مخصص** 🚀

---

## الوصف

هذا المشروع عبارة عن تطبيق Flutter توضيحي يهدف لشرح وتنفيذ آلية **إدارة التنقل (Routing)** بين الشاشات باستخدام Route Manager خاص بالمشروع. يوفر أمثلة واقعية على تنظيم المسارات (routes) للتعامل مع شاشات مثل: المصادقة (`auth`)، الصفحة الرئيسية (`home`)، المنتجات (`product`)، الملف الشخصي (`profile`) والإعدادات (`settings`).

## المزايا الرئيسية ✅

- إدارة مسارات مرنة ومهيكلة بـ Route Manager مخصص. 🔧
- فصل الميزات داخل مجلدات `features` لتسهيل الصيانة والتوسعة. 📂
- دعم منصات متعددة: Android, iOS, Web, Windows, macOS, Linux. 💻📱
- إعداد جاهز لتشغيل المشروع محليًا وكتابة اختبارات Widget.

## المتطلبات المسبقة 🧰

- Flutter مثبت (نسخة مستقرة حديثة). يمكنك التحقق بواسطة:

```bash
flutter --version
```

- الأدوات الخاصة بالمنصة المراد التشغيل عليها (Android SDK, Xcode, إلخ).

## كيفية التشغيل ⚙️

1. استنساخ المستودع:

```bash
git clone <repo-url>
cd zakaria_route_manager
```

2. تنزيل الحزم:

```bash
flutter pub get
```

3. تشغيل التطبيق على المحاكي أو جهاز متصل:

```bash
flutter run
# أو لتشغيل على ويندوز
flutter run -d windows
```

## بنية المشروع 🗂️

- `lib/` - الشيفرة المصدرية للتطبيق
  - `core/` - إعدادات ومساعدات النظام العامّة
  - `features/` - الميزات المنظمة (auth, home, product, profile, settings)
- `android/`, `ios/`, `web/`, `windows/`, `macos/`, `linux/` - إعدادات المنصات
